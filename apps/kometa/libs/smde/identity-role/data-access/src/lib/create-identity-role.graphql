mutation CreateIdentityRole($input: CreateIdentityRoleInput!) {
  createIdentityRole(input: $input) {
    identityRole {
      ...IdentityRole
    }
    errors {
      ... on Error {
        message
      }
    }
  }
}
