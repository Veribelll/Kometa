mutation CreateEmexProduct($input: CreateEmexProductInput!) {
  createEmexProduct(input: $input) {
    emexProduct {
      ...EmexProduct
    }
    errors {
      ... on Error {
        message
      }
    }
  }
}
