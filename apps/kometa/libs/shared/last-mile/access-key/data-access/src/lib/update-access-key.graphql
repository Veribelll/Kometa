mutation UpdateAccessKey($input: UpdateAccessKeyInput!) {
  updateAccessKey(input: $input) {
    accessKey {
      ...AccessKey
    }
    errors {
      ... on Error {
        message
      }
    }
  }
}
